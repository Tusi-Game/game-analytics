/**
 * The package semver, stamped into `sdk.version` on every batch (Q9 provenance).
 * Decoupled from the wire version (`v:1` forever) AND from the stable wire
 * `sdk.name = "analytics-sdk-server"`.
 */
export const SDK_VERSION = '0.1.0';
